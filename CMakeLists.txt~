cmake_minimum_required(VERSION 2.8)
project( opencv_cpp )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )


add_executable( iris_automation_solution iris_automation_solution.cpp )
target_link_libraries( iris_automation_solution ${OpenCV_LIBS} )

